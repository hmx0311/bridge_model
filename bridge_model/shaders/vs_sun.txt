#include "camera_defines.h"
#include "lighting_day_defines.h"

uniform mat3 sunMat;

layout(location = 0) in vec3 vertex;

void main()
{
	gl_Position = camera.projection * vec4(mat3(camera.view) * (sunMat * vertex + 85600.0 * sun.dir), 1.0);
}
